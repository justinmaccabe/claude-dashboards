# Copy to secrets.toml for local Postgres testing, or paste into
# Streamlit Cloud → Settings → Secrets for deployment.
# The app only needs the database; the HubSpot token lives in GitHub Actions.
DATABASE_URL = "postgresql://user:pass@host/dbname"
